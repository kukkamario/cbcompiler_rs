// Implicit declaration with `As` annotation (spec §4.1).

z As String = "asd"
n As Integer = 0
xs As Float[] = New Float[10]
m As MyType = New MyType

// `Ident = expr` without `As` is plain assignment, handled elsewhere.
w = 5
